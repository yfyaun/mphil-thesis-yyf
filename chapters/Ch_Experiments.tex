\chapter{Experimental Evaluation}
\label{ch:experiments}

This chapter evaluates the proposed multimodal sensing-assisted beam-management
framework using the frozen evaluation results.  The evaluation follows the information flow of the
framework: multimodal observations first support target-level detection and beam
prediction; a reliability-gated target-to-user association then determines
whether a sensing-derived candidate set is used; and the resulting measurement
policy is assessed through all-UE effective throughput and CSI-RS overhead.  The
chapter therefore separates learning-level evidence from its communication-level
consequences, rather than interpreting a beam-prediction accuracy alone as a
rate result.

\section{Multimodal V2I Dataset Construction}
\label{sec:experiments-dataset}

\subsection{Multimodal Simulation and Data Generation}
\label{subsec:experiments-data-generation}

The experiments use the existing \texttt{mmbeam\_town05\_3weather\_v1}
multimodal V2I data asset.  In this chapter, \emph{data generation} denotes the
construction of paired learning records and beam-management episodes from this
asset; it does not denote the regeneration of road traffic, images, point
clouds, ray-tracing channels, or other raw simulation outputs.  This distinction
keeps the experimental claims aligned with the available evidence.

At a sensing epoch, the target BS receives its local camera observation, camera
observations from the available neighbouring sensing nodes, and a BS-local ISAC
point cloud.  These inputs form the target-BS-centred multimodal observation used
by the perception network.  The subsequent evaluator replays the corresponding
link evolution, beam labels, and trajectory information to form a local
beam-management episode.  Thus, the perception, association, measurement, and
resource-accounting stages operate on a common scene instance without exposing
ground-truth user identity to the formal decision path.

A sensing sample is indexed by
\((\mathit{weather\_id},\mathit{frame\_id},\mathit{bs\_id})\), where the BS
identifier specifies the target BS.  A link-supervision sample additionally
contains \(\mathit{ue\_id}\).  The 48-beam SSB codebook and 192-beam CSI
codebook are linked through their parent relation.  The perception network
produces a 192-way beam-class posterior for an observed physical target.  Its
ranking is used to score association hypotheses and to reduce the CSI-RS
candidate set; it is neither a UE identifier nor a direct service-beam decision.

\subsection{Scenario Definition and Data Splits}
\label{subsec:experiments-scenario}

The data contain a four-BS roadside scenario under the \emph{Clear},
\emph{Rain/Fog}, and \emph{Night} conditions.  Each target BS serves a local
region and receives complementary observations from neighbouring sensing nodes.
The manifest-defined training, validation, and test splits are retained
throughout the learning and system-level evaluations.  Target tracks and
target-to-user associations are maintained locally at each target BS; the
experiments do not assume a global identity that persists across serving cells.

All reported system results use one frozen radio profile: a 100-ms SSB
periodicity, a 20-ms CSI-RS periodicity, a four-beam conventional refinement
within each SSB parent, and common RT-segment, Doppler, power, noise, scheduling,
regularised zero-forcing (RZF), and resource-accounting assumptions.  The
resource model is a 5G NR-oriented abstraction rather than a full PRB- and
RE-level protocol implementation.  In the Rain/Fog condition,
\(\mathit{min\_association\_margin}=0.10\) is a fixed operating point for the
reported experiments.  It is reported as a configuration of this
evaluation, not as a separate held-out generalisation result.

\section{Evaluation Metrics and Protocol}
\label{sec:experiments-metrics}

\subsection{Detection and Beam-Prediction Metrics}
\label{subsec:experiments-learning-metrics}

Target detection is evaluated by AP@2m and Recall@2m.  The distance qualifier
requires a predicted target to lie within 2~m of its reference target, thereby
making the availability of a useful target-level observation explicit.  For
matched detections, Beam Top-1@2m and Beam Top-4@2m quantify whether the
strongest-beam class is retained by the predicted ranking.  The Top-4 power
ratio reports the fraction of reference link energy captured by the four
highest-ranked CSI beams.  It measures candidate-set utility, not calibration of
the beam-class posterior as a physical power distribution.

The reported learning schemes differ in modality and fusion mechanism:
Camera, ISAC, single-station multimodal, mean-gated multimodal, node-mean
fusion, and cross-agent multimodal.  They are compared under the same
target-level task and the frozen Top-1 cross-entropy beam supervision.  These
comparisons identify how different observations affect detection and ranked beam
candidates; they do not, by themselves, establish a communication-rate gain.

\subsection{System-Level Evaluation Protocol}
\label{subsec:experiments-system-protocol}

The system metric is the all-UE time-average effective throughput,
\begin{equation}
\bar{R}_{\mathrm{eff}}
 =
 \frac{1}{|\mathcal{U}|}
 \sum_{u\in\mathcal{U}}
 \left(
 \frac{1}{N_u}\sum_{n=1}^{N_u} r_{u,n}^{\mathrm{eff}}
 \right),
\label{eq:experiments-effective-rate}
\end{equation}
where \(r_{u,n}^{\mathrm{eff}}\) includes the effect of control, SSB, CSI-RS,
data allocation, and unscheduled intervals at evaluation instant \(n\).
Accordingly, an unserved interval contributes zero throughput.  This definition
prevents conditional scheduled-user rates from obscuring the resource cost of a
measurement policy.

The accompanying operational metrics are CSI-RS overhead, sensing-use ratio,
conventional-fallback ratio, and the mean number of measured candidates.  A
sensing-use event requires a currently observed target and a credible
target-to-user association.  Fallback is therefore an observable consequence of
the association and reliability gate, but it is not a substitute for a
standalone binding-accuracy metric, which is not reported separately here.

The system comparison contains conventional four-beam refinement, conventional
twelve-beam refinement, and the perception-assisted policy.  All three use the
same codebooks, scheduling, RZF processing, and frozen resource profile.  The
perception-assisted policy scans a sensing-derived candidate set only after the
reliability test; otherwise, it executes conventional refinement.  The tracker
comparison applies IMM, KF-CV, and KF-CT to the same Top-1 CE detector and
system protocol.

\section{Multimodal Perception and Beam Prediction}
\label{sec:experiments-perception}

\subsection{Modality and Fusion Results}
\label{subsec:experiments-modality-fusion}

Table~\ref{tab:experiments-modalities} reports the overall results.  No single
fusion scheme is uniformly best across all metrics.  Cross-agent multimodal
fusion yields the highest AP@2m, at \(78.73\%\), while mean-gated multimodal
fusion attains the highest Recall@2m and Top-4 power ratio, at \(81.90\%\) and
\(97.13\%\), respectively.  Single-station multimodal fusion obtains the
highest Beam Top-1@2m, at \(71.54\%\).  This pattern is important: a method
that improves target availability need not improve the exact strongest-beam
class by the same margin, and candidate quality is therefore assessed using
both Top-1 and Top-4 measures.

\begin{table}[t]
    \centering
    \caption{Overall detection and beam-prediction results.  Beam metrics are
    evaluated for detections matched within 2~m.}
    \label{tab:experiments-modalities}
    \resizebox{\textwidth}{!}{%
        \input{paper_results/tables/table_01_learning_modalities_overall.tex}%
    }
\end{table}

The weather-stratified results in Table~\ref{tab:experiments-weather-learning}
expose the conditional nature of modality complementarity.  Under Clear
conditions, cross-agent multimodal fusion reaches \(81.92\%\) AP@2m and
\(75.00\%\) Beam Top-1@2m.  In Rain/Fog, ISAC-only detection
(\(62.12\%\) AP@2m) exceeds camera-only detection (\(43.98\%\)), whereas the
multimodal schemes retain the strongest joint detection and candidate
performance.  At Night, the camera and ISAC results again differ across
detection and beam metrics.  These observations motivate the use of multimodal
evidence and reliability gating, rather than a fixed preference for one
modality or a claim that one fusion rule dominates every condition.

\begin{table}[t]
    \centering
    \caption{Detection and beam-prediction results by environmental condition.
    Beam metrics are evaluated for detections matched within 2~m.}
    \label{tab:experiments-weather-learning}
    \resizebox{\textwidth}{!}{%
        \input{paper_results/tables/table_02_learning_modalities_by_weather.tex}%
    }
\end{table}

\subsection{Value of Nearby Sensing Nodes}
\label{subsec:experiments-nearby-nodes}

Table~\ref{tab:experiments-node-count} and
Figures~\ref{fig:experiments-node-ap}--\ref{fig:experiments-node-beam} vary the
number of available nearby sensing nodes from zero to five for fixed trained
models.  They are consequently missing-node robustness and coverage curves,
rather than comparisons between models retrained at different node counts.

For cross-agent attention, AP@2m rises from \(38.59\%\) with no nearby node to
\(80.41\%\) with five nearby nodes; Recall@2m rises from \(52.34\%\) to
\(83.15\%\).  In contrast, Beam Top-4@2m is already high with few nodes and
then approaches saturation.  The contrast indicates that additional viewpoints
first improve target visibility and localisation.  Their effect on the exact
Top-1 beam ranking is more method dependent, which is consistent with the
different roles of target-BS geometry, modality fusion, and cross-node
observations.

\begin{table}[t]
    \centering
    \caption{Effect of the number of available nearby sensing nodes for fixed
    trained fusion models.}
    \label{tab:experiments-node-count}
    \resizebox{\textwidth}{!}{%
        \input{paper_results/tables/table_03_node_count_overall.tex}%
    }
\end{table}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.90\textwidth]{paper_results/figures/fig_01_node_count_ap.pdf}
    \caption{AP@2m as the number of available nearby sensing nodes varies.}
    \label{fig:experiments-node-ap}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.90\textwidth]{paper_results/figures/fig_02_node_count_recall.pdf}
    \caption{Recall@2m as the number of available nearby sensing nodes varies.}
    \label{fig:experiments-node-recall}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.90\textwidth]{paper_results/figures/fig_03_node_count_beam.pdf}
    \caption{Beam Top-1@2m and Top-4@2m as the number of available nearby
    sensing nodes varies.}
    \label{fig:experiments-node-beam}
\end{figure}

\section{Reliability-Gated Beam Management}
\label{sec:experiments-beam-management}

\subsection{Results Across Weather Conditions}
\label{subsec:experiments-weather-system}

Table~\ref{tab:experiments-system-weather} and
Figure~\ref{fig:experiments-weather-rate} compare the three measurement
policies under each weather condition.  In Clear conditions, the
perception-assisted policy reaches an effective user rate of \(155.453\) Mbps
with \(1.39\%\) CSI-RS overhead, compared with \(151.367\) Mbps and
\(1.90\%\) for conventional four-beam refinement.  The twelve-beam policy
incurs \(5.71\%\) CSI-RS overhead and attains \(127.033\) Mbps.

The sensing-use and fallback columns clarify how the policy reaches these
outcomes.  Sensing candidates are used for \(76.35\%\) of the Clear evaluation
instances, \(56.65\%\) of the Rain/Fog instances, and \(30.07\%\) of the Night
instances.  The corresponding fallback ratios are \(23.65\%\), \(43.35\%\), and
\(69.93\%\).  Thus, when the available observation and association evidence
becomes less reliable, the gate increasingly returns to conventional
refinement instead of applying a sensing-derived hint to a UE unconditionally.

\begin{table}[t]
    \centering
    \caption{Beam-management results by environmental condition under the
    common frozen protocol.  The effective rate is averaged over all UEs.}
    \label{tab:experiments-system-weather}
    \resizebox{\textwidth}{!}{%
        \input{paper_results/tables/table_04_core_by_weather.tex}%
    }
\end{table}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.88\textwidth]{paper_results/figures/fig_04_core_user_rate_by_weather.pdf}
    \caption{Effective user rate by weather condition and beam-management
    policy.}
    \label{fig:experiments-weather-rate}
\end{figure}

\subsection{Macro-Average Resource and Rate Trade-off}
\label{subsec:experiments-macro-system}

Table~\ref{tab:experiments-system-macro} and
Figure~\ref{fig:experiments-system-tradeoff} aggregate the three weather
conditions with equal weight.  The perception-assisted policy achieves an
effective user rate of \(146.332\) Mbps with \(1.56\%\) CSI-RS overhead.  The
corresponding values are \(142.248\) Mbps and \(1.90\%\) for conventional
four-beam refinement, and \(115.890\) Mbps and \(5.71\%\) for conventional
twelve-beam refinement.

The perception-assisted operation remains explicitly conditional: its
macro-average sensing-use ratio is \(54.35\%\), while \(45.65\%\) of instances
use conventional fallback.  The common \(66.75\%\) data-fraction entry in
Table~\ref{tab:experiments-system-macro} follows from the frozen data-side
resource configuration and is not an independently optimised outcome of each
policy.  Taken together, the rate and overhead values show the consequence of
using sensing only when a current detection and a credible target-to-user
association are available.  The service beam is still selected from the
actually measured CSI-RS candidates.

\begin{table}[t]
    \centering
    \caption{Macro-average beam-management results across the three
    environmental conditions.}
    \label{tab:experiments-system-macro}
    \resizebox{\textwidth}{!}{%
        \input{paper_results/tables/table_05_core_macro_average.tex}%
    }
\end{table}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.96\textwidth]{paper_results/figures/fig_05_core_user_rate_hint_overhead.pdf}
    \caption{Macro-average effective rate, CSI-RS overhead, and the
    sensing-use/fallback composition under the frozen protocol.}
    \label{fig:experiments-system-tradeoff}
\end{figure}

\section{Tracking under the Frozen Protocol}
\label{sec:experiments-tracking}

\subsection{Controlled Trajectory Diagnosis}
\label{subsec:experiments-tracker-diagnosis}

Figure~\ref{fig:experiments-tracker-trajectory} visualises KF-CV, KF-CT, and
IMM trajectories for the test, Clear, BS-NE condition with ground-truth
positions perturbed by 1.0-m location noise.  This controlled construction
isolates motion-state estimation from detector misses and association errors.
The figure illustrates how the filters smooth noisy observations and represent
a trajectory containing turning motion.  It is a qualitative diagnostic, not an
end-to-end user-rate comparison or evidence that one tracker is uniformly
superior.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.96\textwidth]{paper_results/figures/fig_06_tracker_gt_noise_test.png}
    \caption{Controlled trajectory diagnosis with ground-truth positions
    perturbed by 1.0-m location noise in the test, Clear, BS-NE condition.}
    \label{fig:experiments-tracker-trajectory}
\end{figure}

\subsection{End-to-End Tracker Comparison}
\label{subsec:experiments-tracker-system}

Table~\ref{tab:experiments-trackers} evaluates the three trackers on the same
Top-1 CE detector and the same frozen system protocol.  Their effective rates
are close: \(146.332\) Mbps for IMM, \(146.373\) Mbps for KF-CV, and
\(145.880\) Mbps for KF-CT.  KF-CV is marginally higher than IMM in this
metric.  The table therefore does not support an effective-rate claim of
universal IMM superiority.

The remaining operational quantities also need to be considered.  IMM uses
sensing candidates in \(54.35\%\) of evaluated instances, with a mean of
\(2.651\) candidates and the same macro-average handover count as the two
reference filters.  IMM is retained in the proposed framework for its
multi-model state-estimation role in association maintenance; the table shows
that its end-to-end consequence must be interpreted jointly with candidate use,
fallback, and the upstream detector, rather than through a single tracker-only
metric.

\begin{table}[t]
    \centering
    \caption{Macro-average end-to-end results for the tracker variants under
    the common detector and frozen protocol.}
    \label{tab:experiments-trackers}
    \resizebox{\textwidth}{!}{%
        \input{paper_results/tables/table_06_tracker_macro_average.tex}%
    }
\end{table}

\section{Discussion and Scope}
\label{sec:experiments-discussion}

The results support a consistent evidence chain.  Multimodal and nearby-node
observations improve different aspects of target availability and beam ranking;
the reliability gate determines whether this evidence can be safely converted
into a narrower CSI-RS measurement action; and the system evaluation reports
the resulting effective throughput together with the associated measurement
cost.  The substantial fallback share under Rain/Fog and Night is consequently
part of the intended operating behaviour, not a failure to be hidden by an
average-rate result.

Several boundaries of interpretation remain.  The reported evaluation does not
include a standalone accepted-binding accuracy, lower-tail rate, or outage
table.  The sensing-use and fallback measures consequently serve as operational
evidence of the association-to-policy interface, without being recast as those
unreported metrics.  In addition, the results concern a local target-BS service
region, existing multimodal data assets, approximately 100-ms RT segments, and
an RZF/resource abstraction.  They characterise the stated 5G NR-oriented
beam-management evaluation, rather than a complete protocol-stack simulation
or a direct real-world deployment result.
